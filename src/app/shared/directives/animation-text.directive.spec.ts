import { AnimationTextDirective } from './animation-text.directive';

describe('AnimationTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationTextDirective();
    expect(directive).toBeTruthy();
  });
});
